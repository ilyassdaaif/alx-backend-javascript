/**
 * Display a message on the STDOUT.
 * @param {String} msg The message to display.
 * @author ilyass daaif <https://github.com/ilyassdaaif>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
